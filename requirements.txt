# Scanner Project Dependencies

# Core dependencies
requests>=2.28.0
Pillow>=9.0.0
psutil>=5.8.0

# Raspberry Pi GPIO dependencies (install only on Pi)
RPi.GPIO>=0.7.1; platform_machine=="armv7l" or platform_machine=="aarch64"
gpiozero>=1.6.2; platform_machine=="armv7l" or platform_machine=="aarch64"

# Display dependencies (install only on Pi)
adafruit-circuitpython-ssd1306>=2.12.0; platform_machine=="armv7l" or platform_machine=="aarch64"
adafruit-blinka>=8.0.0; platform_machine=="armv7l" or platform_machine=="aarch64"

# Optional development dependencies
# Uncomment for development/testing
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0